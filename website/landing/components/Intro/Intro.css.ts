import { styleBlock } from "../../styles/convert"

export const introContainerClassName = styleBlock({
    "@bp2": {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
    },
  });
